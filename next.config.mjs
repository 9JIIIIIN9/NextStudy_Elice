// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["https://elice.io/en/elice/brand"], // 외부 이미지 도메인을 추가합니다.
  },
};

export default nextConfig;
